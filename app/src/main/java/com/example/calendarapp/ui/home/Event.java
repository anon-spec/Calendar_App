package com.example.calendarapp.ui.home;


import java.util.ArrayList;
import java.time.*;
import java.util.HashMap;
import java.util.List;
import java.util.Map;
import com.example.calendarapp.ui.slideshow.calendarActivity;

public class Event {

    /*private String title;
    private  LocalDate date;
    private  LocalDate time;
    Map<LocalDate, List<Event>> eventsMap = new HashMap<>();
    LocalDate eventDate = event.getDate();
    if (!eventsMap.containsKey(eventDate)) {
        eventsMap.put(eventDate, new ArrayList<>());
    }
    eventsMap.get(eventDate).add(event);
    */


}
